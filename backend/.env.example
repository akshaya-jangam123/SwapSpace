# SwapSpace Backend Environment Variables
PORT=5000
# Set your MongoDB connection URI (e.g., MongoDB Atlas in production)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/swapspace?retryWrites=true&w=majority
JWT_SECRET=swapspace_super_secret_jwt_key_production_2026
JWT_EXPIRES_IN=7d
# Comma-separated list or single URL of allowed client origins
CLIENT_URL=https://swap-space-744kper23-akshaya-666d.vercel.app,http://localhost:5173
